import { SuperHeroImpurePipe } from './super-hero-impure.pipe';

describe('SuperHeroImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperHeroImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
